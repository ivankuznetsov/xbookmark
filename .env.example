# This file is the committed template. It MUST stay empty of real
# credentials. Copy it to `.env` (gitignored) and fill values there only.
# Editing this file with real X API credentials and committing it will
# leak them publicly.

# Bookmark source: api (default) | browser | both
#   api     — the X developer API (needs the X_* keys below)
#   browser — a real Chromium reading X's internal GraphQL (no dev API; needs
#             system Chromium + a one-time `xbookmark auth login --browser`)
#   both    — run api then browser in one invocation (resilience/migration)
# In browser mode the X_* keys are optional.
XBOOKMARK_SOURCE=api

# X API (OAuth 2.0 with PKCE)
X_CLIENT_ID=                                    # leave blank here; fill only in .env
X_CLIENT_SECRET=                                # leave blank here; only needed in .env for confidential-client app types
X_REDIRECT_URI=http://127.0.0.1:8765/callback   # register this exact callback URL in X, or change the whole URI
X_USER_ID=                                      # numeric X user id for the bookmarks endpoint

# Where xbookmark creates its standalone bookmark wiki (leading ~ is expanded)
XBOOKMARK_WIKI_PATH=~/wikis/xbookmark

# Whisper transcription
WHISPER_BACKEND=whisper.cpp   # or faster-whisper
WHISPER_MODEL=base.en         # tiny.en, base.en, small.en, medium.en, tiny, base, small, medium, or large-v3
WHISPER_THREADS=              # optional whisper.cpp thread count; defaults to up to 8 CPU threads

# codex CLI profile for enrichment
CODEX_PROFILE=default

# Optional: generate LLM summaries for author/topic/entity landing pages.
# Bookmark notes are always enriched; enabling this adds many extra LLM calls.
XBOOKMARK_AUX_SUMMARIES=false
